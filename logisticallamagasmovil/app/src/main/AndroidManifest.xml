<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kalla.enterprise.llamagas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Logisticallamagasmovil">
        <activity
            android:name=".gui.ActSplash"
            android:exported="true" />
        <activity
            android:name=".movil.facturacion.gui.ActPrinterManager"
            android:exported="true" />
        <activity
            android:name="com.example.tscdll.TSCActivity"
            android:exported="true"/>

        <activity
            android:name=".gui.PermisosActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>